# 安装 Chrome 扩展

1. 打开 Chrome，在地址栏输入 `chrome://extensions/`。
2. 打开右上角“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择整个 `chrome-extension` 文件夹。
5. 打开桌面 App，确认端口为 `8765`，填写 API Key 后点击“启动扩展服务”。
6. 打开或刷新 `https://web.whatsapp.com/`。

扩展加载时会忽略页面中已有的历史消息，只处理加载完成后新收到的消息。
